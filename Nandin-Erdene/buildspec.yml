version: 0.2

phases:
  install:
    commands:
      - python -m pip install --upgrade pip
      - pip install -r requirements.txt

  build:
    commands:
      - cdk synth

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'
